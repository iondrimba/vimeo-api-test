sudo: required
language: node_js
before_install:
 - gem install sass
 - gem install ruby 
 - gem install scss_lint
before_script:
  - npm -g install npm@latest
  - npm install -g gulp
script: gulp travis