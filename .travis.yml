sudo: required
language: node_js
before_install:
 - gem install sass
 - gem install ruby 
 - gem install scss_lint
 - npm install -g npm@2
 - npm install -g npm
before_script:
  - npm install -g gulp
script: gulp travis